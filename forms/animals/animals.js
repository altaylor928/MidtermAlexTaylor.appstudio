//create array named myAnimals
let myAnimals = ['dog', 'cat', 'horse', 'meerkat']

//gets name of an animal from user and adds it to end of array
let userAnimals = prompt("Enter an animal to add it to the array: ")
      userAnimals = userAnimal.toLowerCase()
myAnimals.push(userAnimal)

//outputs the last animal in the array to an alert
let lastAnimal = myAnimals.slice(-1)[0])
alert(`The last animal is a/an ${lastAnimal}.`)